version: '2'

services:
    
    redis:
      build: ./redis
      privileged: true
      environment:
       - DEBUG=aiziyuer  
    
    nginx:
      build: ./nginx
      privileged: true
      environment:
       - DEBUG=aiziyuer
    